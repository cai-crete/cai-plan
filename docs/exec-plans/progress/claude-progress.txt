[2026-04-22] sketch-to-plan — N17b: 알약 바 휴지통 — 아트보드 내 이미지만 삭제로 변경

## 방금 무엇을 했는지

알약 바 Delete 버튼 동작 변경:
- **이전**: `handleDeleteItem(id)` → 아트보드 전체 삭제
- **이후**: `src`·`contentTransform`만 제거 → 아트보드는 유지, 이미지만 삭제

```ts
setHistoryStates(h => [...h, canvasItemsRef.current]);
setRedoStates([]);
setCanvasItems(prev => prev.map(i =>
  i.id === id ? { ...i, src: undefined, contentTransform: undefined } : i
));
imageEditSnapshot.current = null;
setImageEditingId(null);
setSelectedItemIds([]);
```

- undo/redo 지원 (history push)
- 편집 모드 종료 + 선택 해제

## 어디까지 완료됐는지

### 완료 ✓
- N05~N11: 이전 버그 수정들 ✓
- N14: X 버튼 클릭 → confirm + deselect ✓
- N15: 이미지 편집 모드 중 아트보드 배경 클릭 → 이동 차단 ✓
- N16: 이미지 편집 모드 이동 작동 + undo/redo 지원 ✓
- N17: 알약 바 Delete 버튼 추가 ✓
- N17b: 알약 바 Delete → 이미지만 삭제(아트보드 유지)로 변경 ✓

## 다음 세션 후보
- 실제 API 테스트 (모델 ID 검증/조정)
- 지적도(cadastral) 레이어 업로드 UI
- 그리드(grid) 오버레이 시각화
- rotation 적용 후 AI generate 시 flatten 처리 검증
