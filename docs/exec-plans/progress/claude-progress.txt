[2026-04-21] sketch-to-plan — N04: Generated Image Metadata + Sidebar Context

## 방금 무엇을 했는지

### 1. exec-plan N04 생성
- 파일: docs/exec-plans/active/N04-exec-plan-generated-image-metadata-2026-04-21.md

### 2. 생성 이미지에 파라미터 저장
- page.tsx의 generatedPlanImage useEffect에서 planItem 생성 시
  `planItem.parameters`에 3개 값 저장:
  - `parameterReport`: roomAnalysis (AI 분석 텍스트)
  - `floorType`: 생성 당시 선택된 건물 타입
  - `gridModule`: 생성 당시 선택된 그리드 모듈 (mm)
- 수정 파일: sketch-to-plan/src/app/page.tsx (~line 273)

### 3. selectedItemIds 연동 effect 추가
- 위치: page.tsx — parameterReport 선언 직후
- 동작:
  - sketch_generated 클릭 → item.parameters에서 floorType, gridModule, parameterReport 복원 → 사이드바에 표시
  - 빈 캔버스 클릭 (selectedItemIds = []) → parameterReport=null, floorType='', gridModule=4000 (초기화)
  - artboard/upload 클릭 → 사이드바 상태 변경 없음 (현재 입력 보존)

## 어디까지 완료됐는지

### 완료 ✓
- product-spec ✓
- Protocol + 3개 knowledge docs (_context/) ✓
- API route ✓
- Hook ✓
- SketchToPlanPanel UI (순서 + PARAMETER REPORT) ✓
- RightSidebar ✓
- canvas.ts LayerType ✓
- page.tsx (sourceArtboardSize, 텍스트 아이템 제거) ✓
- sketch-to-image 잔재 완전 제거 ✓
- Loop B Iter 2 PASS ✓
- PARAMETER REPORT 버그 수정 (parameterReport 상태 분리) ✓
- 패널 스타일 가이드 문서 출력 ✓
- N## exec-plan 리비전 관리 도입 ✓
- 생성 이미지 파라미터 저장 (planItem.parameters) ✓
- sketch_generated 클릭 시 사이드바 파라미터 복원 ✓
- 빈 캔버스 클릭 시 사이드바 초기화 ✓

### 미구현 (다음 세션 후보)
- 실제 API 테스트 후 모델 ID 검증/조정
  - route.ts line 5-8: MODEL_ANALYSIS, MODEL_IMAGE_GEN, MODEL_ANALYSIS_FB, MODEL_IMAGE_GEN_FB
  - gemini-3.1-pro-preview / gemini-3.1-flash-image-preview는 프리뷰 ID — 실제 응답 확인 필요
- 지적도(cadastral) 레이어 업로드 UI (layerType='cadastral' 아이템)
- 그리드(grid) 오버레이 시각화 (layerType='grid' 아이템)

## 다음에 무엇을 해야 하는지

### 우선순위 1: 실제 API 테스트
- `npm run dev` 실행
- 아트보드에 버블 다이어그램 스케치 후 GENERATE 테스트
- 생성된 이미지 클릭 → 사이드바에 BuildingType, GridModule, ParameterReport 표시 확인
- 빈 캔버스 클릭 → 사이드바 초기화 확인
- 모델 ID 오류 시: gemini-3.1-xxx → 실제 사용 가능 ID로 교체

### 우선순위 2: 지적도 레이어 기능 (선택)
- 지적도 이미지를 layerType='cadastral' 아이템으로 임포트
- 레이어 위계 UI: sketch > cadastral > grid 순서 시각화
