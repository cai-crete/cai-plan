[2026-04-21] sketch-to-plan — N03: PARAMETER REPORT 버그 수정 + 패널 스타일 출력 + N## 리비전 관리 도입

## 방금 무엇을 했는지

### 1. exec-plan 파일명 N## 리비전 관리 도입
- 신규 exec-plan은 N##- 접두사를 붙여 생성
- 기존 파일: exec-plan-sketch-to-plan-setup.md (N01 해당)
- 기존 파일: exec-plan-panel-layout-imagesize-2026-04-21.md (N02 해당)
- 이번 문서: docs/exec-plans/active/N03-exec-plan-ui-fixes-2026-04-21.md

### 2. PARAMETER REPORT 버그 수정
- 근본 원인: page.tsx useEffect에서 resetGeneration() 호출 시 hook의 roomAnalysis가 null로 초기화
  → 패널에 분석 결과가 표시되지 않던 현상
- 수정: page.tsx에 `parameterReport` 상태 추가
- resetGeneration() 호출 전 `if (roomAnalysis) setParameterReport(roomAnalysis)` 로 값 capture
- RightSidebar에 roomAnalysis 대신 parameterReport 전달
- 수정 파일: sketch-to-plan/src/app/page.tsx (3곳)

### 3. 패널 스타일 문서 출력
- 목적: 다른 CAI 노드 앱에서 우측 패널 스타일을 재사용할 수 있도록 문서화
- 출력 파일: docs/report/right-panel-style-guide.md
- 포함 내용: 컨테이너, 섹션 레이블, textarea, dropdown, range slider, report 박스,
  GENERATE 버튼 (4개 상태), copyright, 전체 레이아웃 구조

## 어디까지 완료됐는지

### 완료 ✓
- product-spec ✓
- Protocol + 3개 knowledge docs (_context/) ✓
- API route ✓
- Hook ✓
- SketchToPlanPanel UI (순서 + PARAMETER REPORT) ✓
- RightSidebar ✓
- canvas.ts LayerType ✓
- page.tsx (sourceArtboardSize, 텍스트 아이템 제거) ✓
- sketch-to-image 잔재 완전 제거 ✓
- Loop B Iter 2 PASS ✓
- PARAMETER REPORT 버그 수정 (parameterReport 상태 분리) ✓
- 패널 스타일 가이드 문서 출력 ✓
- N## exec-plan 리비전 관리 도입 ✓

### 미구현 (다음 세션 후보)
- 실제 API 테스트 후 모델 ID 검증/조정
  - route.ts line 5-8: MODEL_ANALYSIS, MODEL_IMAGE_GEN, MODEL_ANALYSIS_FB, MODEL_IMAGE_GEN_FB
  - gemini-3.1-pro-preview / gemini-3.1-flash-image-preview는 프리뷰 ID — 실제 응답 확인 필요
- 지적도(cadastral) 레이어 업로드 UI (layerType='cadastral' 아이템)
- 그리드(grid) 오버레이 시각화 (layerType='grid' 아이템)

## 다음에 무엇을 해야 하는지

### 우선순위 1: 실제 API 테스트
- `npm run dev` 실행
- 아트보드에 버블 다이어그램 스케치 후 GENERATE 테스트
- PARAMETER REPORT에 roomAnalysis 텍스트 출력 확인 (버그 수정 검증)
- 생성 이미지가 아트보드 사이즈와 일치하는지 확인
- 모델 ID 오류 시: gemini-3.1-xxx → 실제 사용 가능 ID로 교체

### 우선순위 2: N## 리비전 관리 적용
- 기존 completed/ 파일을 N01, N02로 rename할지 여부 결정 (사용자 판단 필요)
- 이후 모든 신규 exec-plan은 N##- 접두사 필수 적용

### 우선순위 3: 지적도 레이어 기능 (선택)
- 지적도 이미지를 layerType='cadastral' 아이템으로 임포트
- 레이어 위계 UI: sketch > cadastral > grid 순서 시각화
